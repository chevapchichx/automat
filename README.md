# Hotel Room Booking App (Automat)

Мобильное Android-приложение для просмотра и бронирования номеров отеля. Разработано на Kotlin с использованием Jetpack Compose и локальной базы данных SQLite.

## 📱 Описание проекта

Приложение позволяет пользователям:
- Регистрироваться и входить в систему
- Просматривать доступные номера отеля с фотографиями
- Искать номера по фильтрам (даты заезда/выезда, количество гостей)
- Просматривать детальную информацию о каждом номере (описание, удобства, цена)
- Фильтровать номера по статусу (все / доступные / занятые)

## 🏗️ Архитектура проекта

### Структура пакетов

```
com.example.automat/
├── MainActivity.kt              # Главная Activity с UI экранами (Compose)
├── data/
│   ├── Models.kt               # Data классы (User, RoomItem)
│   ├── DatabaseHelper.kt       # SQLite helper для работы с БД
│   └── Repository.kt           # Слой репозитория для доступа к данным
└── ui/
    └── theme/                  # Тема приложения (Material 3)
        ├── Color.kt
        ├── Theme.kt
        └── Type.kt
```

### Технологии

- **Язык**: Kotlin
- **UI**: Jetpack Compose + Material 3
- **База данных**: SQLite (SQLiteOpenHelper)
- **Минимальная версия Android**: API 24 (Android 7.0)
- **Target SDK**: API 36

## 🗄️ База данных

### Схема БД (версия 3)

#### Таблица `users`
| Поле      | Тип     | Описание                    |
|-----------|---------|----------------------------|
| id        | INTEGER | Первичный ключ (AUTO_INCREMENT) |
| username  | TEXT    | Логин (UNIQUE)             |
| password  | TEXT    | Пароль                     |
| fullname  | TEXT    | ФИО пользователя           |

#### Таблица `rooms`
| Поле        | Тип     | Описание                           |
|-------------|---------|-----------------------------------|
| id          | INTEGER | Первичный ключ (AUTO_INCREMENT)    |
| number      | TEXT    | Номер комнаты (101, 102 и т.д.)   |
| capacity    | INTEGER | Вместимость (кол-во человек)       |
| price       | REAL    | Цена за сутки (₽)                 |
| description | TEXT    | Описание номера                    |
| isAvailable | INTEGER | Доступность (1 - да, 0 - нет)     |
| amenities   | TEXT    | Удобства (WiFi, ТВ и т.д.)        |
| imageRes    | TEXT    | Имя ресурса изображения            |

### Миграции

- **Версия 1**: Базовая схема (users, rooms без amenities/imageRes)
- **Версия 2**: Добавлено поле `amenities` для удобств в номерах
- **Версия 3**: Добавлено поле `imageRes` для хранения ссылок на изображения

### Тестовые данные

**Пользователи:**
- Логин: `1`, Пароль: `1` (Иван Иванов)
- Логин: `2`, Пароль: `2` (Пётр Петров)

**Номера:**
- **101**: 2 чел, 1500₽/сутки, WiFi/Кондиционер/ТВ, доступен
- **102**: 4 чел, 2500₽/сутки, WiFi/Кондиционер/ТВ/Холодильник, доступен
- **201**: 2 чел, 1200₽/сутки, WiFi/ТВ, занят
- **202**: 3 чел, 1800₽/сутки, WiFi/Кондиционер/ТВ/Минибар, доступен

## 🎨 Экраны приложения

### 1. **Экран входа (Login)**
- Поля для ввода логина и пароля
- Кнопка "Войти"
- Ссылка на регистрацию
- Валидация: отображается ошибка при неверных учетных данных

### 2. **Экран регистрации (Register)**
- Поля: ФИО, логин, пароль
- Кнопка "Зарегистрироваться"
- Кнопка "Назад"

### 3. **Главный экран (Home)**
- Приветствие с ФИО пользователя
- Три кнопки:
  - "Выйти из учётной записи"
  - "Поиск номеров по фильтрам"
  - "Просмотр всех номеров"

### 4. **Экран фильтров поиска (SearchFilter)**
- Выбор даты заезда (DatePicker)
- Выбор даты выезда (DatePicker)
- Ввод количества гостей
- Кнопки "Назад" и "Найти"

### 5. **Экран результатов поиска (SearchResults)**
- Список доступных номеров, подходящих по критериям
- Отображение выбранных дат и количества гостей
- Кнопка "Назад"
- При клике на номер → переход к детальной информации

### 6. **Экран всех номеров (AllRooms)**
- Список всех номеров
- Фильтры: Все / Доступные / Недоступные
- Кнопка "Назад"
- При клике на номер → переход к детальной информации

### 7. **Детальная информация о номере (RoomDetail)**
- Фотография номера
- Номер, вместимость, цена, статус
- Блок "Удобства" (WiFi, ТВ и т.д.)
- Описание
- Кнопка "Назад" (возвращает на предыдущий экран: поиск или список всех)

## 🚀 Запуск проекта

### Требования
- Android Studio Hedgehog (2023.1.1) или новее
- JDK 11+
- Android SDK (API 24-36)

### Шаги установки

1. Клонируйте или откройте проект в Android Studio:
   ```bash
   cd /Users/svetatugay/AndroidStudioProjects/automat
   ```

2. Синхронизируйте Gradle:
   ```
   File → Sync Project with Gradle Files
   ```

3. Запустите приложение:
   - На эмуляторе: `Run → Run 'app'`
   - На устройстве: подключите Android-устройство с включенной отладкой по USB

4. Или соберите APK через терминал:
   ```bash
   ./gradlew :app:assembleDebug
   ```
   APK будет находиться в `app/build/outputs/apk/debug/app-debug.apk`

5. Установите на устройство:
   ```bash
   ./gradlew :app:installDebug
   ```

## 📸 Изображения номеров

Изображения номеров хранятся в `app/src/main/res/drawable-nodpi/`:
- `room_101.jpg`
- `room_102.jpg`
- `room_201.jpg`
- `room_202.jpg`

Для добавления новых изображений:
1. Поместите файл в `app/src/main/res/drawable-nodpi/`
2. Используйте формат имени: `room_XXX.jpg` (где XXX - номер комнаты)
3. Добавьте запись в БД с соответствующим значением поля `imageRes`

## 🔧 Конфигурация

### Gradle

Основные зависимости определены в `gradle/libs.versions.toml`:
- Kotlin 2.0.21
- Compose BOM 2024.09.00
- Material 3

### Сборка

```kotlin
// app/build.gradle.kts
android {
    compileSdk = 36
    minSdk = 24
    targetSdk = 36
    
    buildFeatures {
        compose = true
    }
}
```

## 📝 Особенности реализации

### Навигация
Используется state-based навигация через sealed class `Screen`:
- Сохраняется стек навигации для корректного возврата
- Запоминаются параметры поиска при переходе к деталям номера

### Date Picker
Интеграция нативного Android DatePickerDialog для выбора дат заезда/выезда.

### Динамическая загрузка изображений
Изображения загружаются по имени ресурса из БД через `context.resources.getIdentifier()`.

### Обработка ошибок
- Валидация логина/пароля с отображением сообщения об ошибке
- Проверка на пустой список номеров
- Fallback на изображение-заглушку при отсутствии фото

## 🎯 Возможные улучшения

- [ ] Добавить таблицу бронирований для реальной проверки доступности по датам
- [ ] Реализовать функционал бронирования номера
- [ ] Добавить Room Database вместо SQLiteOpenHelper
- [ ] Внедрить ViewModel + StateFlow для управления состоянием
- [ ] Добавить удаленный API для синхронизации данных
- [ ] Реализовать профиль пользователя с историей бронирований
- [ ] Добавить push-уведомления о статусе бронирования
- [ ] Поддержка нескольких языков (локализация)

## 👨‍💻 Автор

Разработано как учебный проект для демонстрации работы с:
- Jetpack Compose
- SQLite в Android
- Material Design 3
- MVVM-подобной архитектуры

## 📄 Лицензия

Проект создан в образовательных целях.

---

**Дата создания**: 24 ноября 2025 г.
